/*
 * TinyUSB configuration for PWM Servo emulator.
 *
 * This firmware uses pico-sdk's built-in stdio USB CDC (pico_enable_stdio_usb),
 * so no custom TinyUSB configuration is needed. This file is provided for
 * completeness and to allow future customization if needed.
 */

#ifndef TUSB_CONFIG_H
#define TUSB_CONFIG_H

/* Using pico-sdk stdio_usb which handles TinyUSB configuration internally.
 * No custom defines needed here. */

#endif /* TUSB_CONFIG_H */
